---
interface Props {
  href: string;
  openInNewTab?: boolean;
}

const { href, openInNewTab = true } = Astro.props;
---

<a
  class="font-medium"
  href={href}
  {...openInNewTab && { rel: "noopener noreferrer", target: "_blank" }}><slot /></a
>
