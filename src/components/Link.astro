---
interface Props {
  href: string;
  external?: boolean;
}

const { href, external: external = true } = Astro.props;
---

<a
  class="font-medium"
  href={href}
  {...external && { rel: "noopener noreferrer", target: "_blank" }}><slot /></a
>
